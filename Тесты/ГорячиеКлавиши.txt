"C:\Program Files (x86)\Norton Internet Security\Engine\22.15.0.88\uistub.exe" /win8
"C:\Program Files (x86)\VMware\VMware Workstation\vmware.exe"
*100*1#
Сtrl + Shift + c выделить обьявление метода
Сtrl + k + f выделяешь 1 оператор и бахаещь
Сtrl + k + d na vse?
Сtrl + a videlit
Win + shift + s
Win + R
for (1;2;3) все части необязательные! 
1-инициализация может быть перед циклом;(если иницализировал перед циклом изменение управляющей переменной сохраниться!
можно брать несколько переменных!
2-условие выхода;
может брать несколько условий!
3-изменение управляющей переменной (можно делать не в шапке а в любой части тела)

#include <ctime> 

goto link;
...
link:

int arr[] = {1, 22 , 453,2};
int arr[]{1, 22 , 453,2};
int arr[][]

sizeof(arr)/sizeof(int)

#include <ctime>
a=rand();%10 ограничеть 
a=rnad()%10 + 5
srand(time(NULL));

return result;
return a+b;

inline встраивание а не вызов (можно пистаь для мелких функций) писать в шапке перед типом

template <typename T1, typename T2>
T1 sum(T1 a,T2 b) будет работать функция со всеми типами передаваемыми
int sum(T1 a,T2 b)

стек - полсденяя функция зашла а выполнилась первая( гора посуды в сосуде)

bool isCorrect = true;
setlocale(LC_ALL,"Rus")
/* */
std :: cout //сout содержится в пространстве имен стд
"\\" вывод обратного слеша
"\"" знака кавычек
"\'" одинарные кавычки
++a инкримент перед другими операциями
a-- декремент после
a += 20 kak a= a+20
== равно
!= неравно
&& и
|| или
!= не
% ОСТАТОК от деления